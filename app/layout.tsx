export const metadata = { title: 'GDB' }
export default function RootLayout({children}:{children:any}){
  return(<html lang="fr"><body style={{margin:0,background:'#0f172a'}}>{children}</body></html>)
}
